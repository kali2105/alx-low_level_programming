#include "main.h"

/**
 * mul - A function that multiplies two numbers
 * @a: The first number to multiplie
 * @b: The second number to multiplie with
 *
 * Return: the function mul
 */
int mul(int a, int b)
{
	return (a * b);
}
